(function(){
  const configText=document.getElementById('site-config')?.textContent?.trim();let siteConfig={basePath:''};try{if(configText)siteConfig=JSON.parse(configText)}catch(_){ }const basePath=String(siteConfig.basePath??'').replace(/\/$/,'');
  function setTheme(theme){const resolved=theme==='light'?'light':'dark';document.documentElement.dataset.theme=resolved;localStorage.setItem('egxalpha-theme',resolved);document.querySelectorAll('[data-theme-toggle]').forEach(b=>b.setAttribute('aria-pressed',resolved==='light'?'true':'false'))}
  setTheme(localStorage.getItem('egxalpha-theme')||'dark');document.querySelectorAll('[data-theme-toggle]').forEach(b=>b.addEventListener('click',()=>setTheme(document.documentElement.dataset.theme==='light'?'dark':'light')));
  function initModelOutput(){const search=document.querySelector('[data-model-search]');const filters=[...document.querySelectorAll('[data-model-filter]')];const rows=[...document.querySelectorAll('[data-model-row]')];const empty=document.querySelector('[data-model-empty]');if(!rows.length)return;let active='all';function render(){const q=(search?.value||'').trim().toLowerCase();let visible=0;rows.forEach(row=>{const direction=row.dataset.direction||'neutral';const text=row.dataset.searchText||'';const show=(active==='all'||direction===active)&&(!q||text.includes(q));row.hidden=!show;if(show)visible++});if(empty)empty.hidden=visible!==0}search?.addEventListener('input',render);filters.forEach(button=>button.addEventListener('click',()=>{active=button.dataset.modelFilter||'all';filters.forEach(x=>x.classList.toggle('active',x===button));render()}));render()}
  initModelOutput();
  async function initSearch(){const input=document.querySelector('[data-search-input]');const output=document.querySelector('[data-search-results]');if(!input||!output)return;let rows=[];try{const res=await fetch(`${basePath}/data/index.json`,{cache:'no-cache'});if(!res.ok)throw new Error();rows=await res.json()}catch(_){output.innerHTML='<p class="small-note">Search index is unavailable.</p>';return}function render(){const q=input.value.trim().toLowerCase();const filtered=rows.filter(row=>!q||Object.values(row).join(' ').toLowerCase().includes(q)).slice(0,50);output.innerHTML=filtered.length?filtered.map(row=>`<a class="archive-row" href="${basePath}${row.url}"><span>${row.date||''}</span><strong>${row.display_symbol||row.symbol||''}</strong><em>${row.company_name||row.sector||row.horizon||''}</em><small>${String(row.plain_direction||row.direction_bucket||'').replaceAll('_',' ')}</small></a>`).join(''):'<p class="small-note">No matching records.</p>'}input.addEventListener('input',render);render()}
  initSearch();
  if('serviceWorker'in navigator){window.addEventListener('load',()=>{navigator.serviceWorker.getRegistrations?.().then(rs=>rs.forEach(r=>r.unregister())).catch(()=>{})})}
})();
