import { escapeHtml, siteFooter, siteHeader } from './templates.mjs';

const METHODOLOGY_CSS = String.raw`
.page-methodology .research-paper{width:min(100%,1080px);margin:36px auto 0;border:1px solid var(--line);border-radius:12px;background:var(--surface);overflow:hidden}
.page-methodology .research-cover{padding:clamp(34px,6vw,68px);border-bottom:1px solid var(--line)}
.page-methodology .research-cover h1{max-width:13ch;margin:12px 0 18px}
.page-methodology .research-cover .lede{margin-bottom:0}
.page-methodology .doc-control{display:grid;grid-template-columns:repeat(4,1fr);margin-top:30px;border:1px solid var(--line);border-radius:9px;overflow:hidden}
.page-methodology .doc-control div{padding:13px;border-right:1px solid var(--line)}
.page-methodology .doc-control div:last-child{border-right:0}
.page-methodology .doc-control span{display:block;color:var(--muted);font-family:var(--mono);font-size:.62rem;letter-spacing:.07em}
.page-methodology .doc-control strong{display:block;margin-top:6px;font-family:var(--mono);font-size:.78rem}
.page-methodology .method-intro{padding:28px clamp(24px,6vw,68px);border-bottom:1px solid var(--line);background:var(--surface-2)}
.page-methodology .method-intro>p{max-width:82ch;color:var(--soft)}
.page-methodology .research-body{padding:14px clamp(24px,6vw,68px) 48px}
.page-methodology .paper-section{display:grid;grid-template-columns:68px 1fr;gap:22px;padding:34px 0;border-bottom:1px solid var(--line)}
.page-methodology .paper-section:last-child{border-bottom:0}
.page-methodology .paper-section-index{color:var(--blue);font-family:var(--mono);font-size:.75rem}
.page-methodology .paper-section-copy h2{font-size:1.4rem}
.page-methodology .paper-section-copy p{max-width:80ch;color:var(--soft);font-size:.94rem}
.page-methodology .paper-section-copy p:last-child{margin-bottom:0}
.page-methodology .paper-note{margin-top:18px;padding:15px 17px;border-left:3px solid var(--blue);background:var(--surface-2);color:var(--muted);font-size:.84rem}
.page-methodology .paper-boundary{padding:34px clamp(24px,6vw,68px);border-top:1px solid var(--line);background:var(--surface-2)}
.page-methodology .boundary-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-top:18px}
.page-methodology .boundary-grid article{padding:18px;border:1px solid var(--line);border-radius:9px;background:var(--surface)}
.page-methodology .boundary-grid p{margin:7px 0 0;color:var(--muted);font-size:.84rem}
@media(max-width:760px){.page-methodology .research-cover h1{max-width:none;font-size:clamp(2.15rem,9vw,2.65rem);line-height:1.02;letter-spacing:-.045em}.page-methodology .doc-control{grid-template-columns:1fr 1fr}.page-methodology .paper-section{grid-template-columns:1fr;gap:8px}.page-methodology .boundary-grid{grid-template-columns:1fr}}
@media print{@page{size:A4;margin:15mm}.page-methodology .topbar,.page-methodology .site-footer{display:none!important}.page-methodology .site-shell{width:100%;padding:0}.page-methodology .research-paper{margin:0;border:0}.page-methodology .paper-section,.page-methodology .paper-boundary{break-inside:avoid}}
`;
function control(label, value) { return `<div><span>${escapeHtml(label)}</span><strong>${escapeHtml(value)}</strong></div>`; }
function summary(n,title,body){return `<article><span>${escapeHtml(n)}</span><strong>${escapeHtml(title)}</strong><p>${escapeHtml(body)}</p></article>`;}
function section(n,title,paragraphs,note=''){return `<section class="paper-section" id="r${n}"><div class="paper-section-index">${escapeHtml(n)}</div><div class="paper-section-copy"><h2>${escapeHtml(title)}</h2>${paragraphs.map(p=>`<p>${escapeHtml(p)}</p>`).join('')}${note?`<aside class="paper-note">${escapeHtml(note)}</aside>`:''}</div></section>`;}
export function methodologyPage() {
  return `<style id="methodology-document-styles">${METHODOLOGY_CSS}</style><main class="site-shell page-methodology" data-page="methodology">${siteHeader('RESEARCH', 'research')}<article class="research-paper paper-document"><header class="research-cover paper-cover"><p class="eyebrow">HOW /ALPHA WORKS</p><h1>A quantitative ranking system, not a recommendation engine.</h1><p class="lede">EGX /Alpha compares eligible Egyptian listed stocks across four forward trading-day outlooks. The public interface shows relative rank, rank percentile, rank movement and a separate directional signal while proprietary model scores remain private.</p><div class="doc-control">${control('MARKET','EGYPTIAN EXCHANGE')}${control('PUBLIC OUTLOOKS','1D / 3D / 5D / 10D')}${control('DEFAULT VIEW','5D')}${control('USE','RESEARCH ONLY')}</div></header><section class="method-intro"><p class="eyebrow">THE FIVE-STEP PROCESS</p><div class="method-summary">${summary('01','Observe','Use completed post-close market observations.')}${summary('02','Validate','Enforce freshness, eligibility and time availability.')}${summary('03','Infer','Apply the reviewed deep-learning model package.')}${summary('04','Rank','Order the eligible domestic universe by outlook.')}${summary('05','Publish','Release the sanitized public model record.')}</div><div class="interpretation-card"><h2>Rank and signal answer different questions.</h2><div class="interpretation-grid"><article><strong>Rank / percentile</strong><p>Where does this stock sit relative to the other eligible stocks for the selected outlook?</p></article><article><strong>Positive / Neutral / Negative</strong><p>What directional bucket does the model assign? A high rank can still carry a Negative signal when the wider universe is weak.</p></article></div></div></section><div class="research-body">${section('01','Research objective: relative opportunity', ['The central problem is cross-sectional. On each eligible trading date the system compares Egyptian listed securities with one another and estimates their relative forward position. The public rank is a position within that day’s eligible comparison set, not a guaranteed return or valuation target.','The public interface labels the native 1, 3, 5 and 10-session horizons as 1D, 3D, 5D and 10D, where D means trading days. Five trading days remains the default presentation.'])}${section('02','Quantitative public interpretation', ['Rank percentile is derived only from public rank and public eligible-universe size. It normalises relative position when the comparison universe changes slightly from session to session. It is not a probability, confidence score or hidden model magnitude.','Rank movement compares the current position only with the previous completed session at the same outlook. Direction is published separately as Positive, Neutral or Negative.'], 'No public percentile or movement measure exposes the private ranking score, return forecast or direction logit.')}${section('03','Observation discipline and time lock', ['Live inference begins with source-aware market observation. Only information demonstrably available by the Cairo-time decision cutoff can enter the research frame. Freshness, valid prices, active trading and adequate history are eligibility conditions.','Missing or stale observations lead to explicit exclusion rather than invented replacement data.'])}${section('04','Deep-learning ranking layer', ['A reviewed neural ranking model produces comparable estimates across the eligible securities and orders the universe independently for the 1, 3, 5 and 10-session forward research windows. Production inference applies the fixed reviewed package; it does not retrain the network or relax data-quality requirements during the daily run.'])}${section('05','Validation and evidence discipline', ['Model development uses date-ordered separation and held-out evaluation. Candidate packages are evaluated for ranking behaviour, robustness, runtime parity and operational integrity before promotion. Live realised evidence is tracked separately and remains maturity-gated before live ranking metrics are shown.'])}${section('06','Public publication boundary', ['The public layer is a one-way disclosure boundary. It publishes sanitized full-universe rankings for the four native outlooks together with date, rank, direction, source state, provenance, bounded research evidence and a research disclaimer while withholding private numerical scores and implementation details.','Historical records remain date-addressable so the system can be inspected longitudinally without rewriting past model outputs.'])}${section('07','Interpretation limits', ['EGX /Alpha is a research-ranking engine operating under changing market regimes and source availability. A rank is conditional on the eligible universe, selected outlook and information available at the cutoff. It is not personalised investment advice, a target price or an execution instruction.'])}</div><section class="paper-boundary"><p class="eyebrow">DISCLOSURE BOUNDARY</p><h2>What is public and what remains private</h2><div class="boundary-grid"><article><strong>Public research surface</strong><p>Completed analysis date, eligible-universe context, 1D / 3D / 5D / 10D rank views, rank percentile, same-outlook movement, public direction category, source/publication state, bounded model evidence, archive provenance and methodology.</p></article><article><strong>Private research implementation</strong><p>Raw source records, exact feature construction, model dimensions, objective coefficients, trained weights, private ranking scores, return forecasts, direction logits, internal diagnostics and operational paths.</p></article></div></section></article>${siteFooter()}</main>`;
}
